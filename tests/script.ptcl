prototype function printf(content: character*, ...): integer
prototype function printf_number(content: integer): integer
prototype function putchar(content: character): integer

typedata statement(type: integer)

syntax list([name: static word]) end {
    { typedata !name() }
    !name
}

function main(): integer {
    other_name: list(test) = test()
    value: static word = left
    value = !value

    function do_something_with_variables(): void {
        typedata here()

        function another(): void {
            example := here()
            value = yet
        }

        value = joke
        value = another
    }

    do_something_with_variables()
    return 0
}
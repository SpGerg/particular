prototype function printf(content: character*, ...): integer
typedata statement(statement_type: integer)

type string: character*

syntax exceptional end {
	@[public_attribute]
}

exceptional function main(): integer {
	syntax Ref([value: any]) end {
		static if value is >pointer {
			static if value == null {
				null
			}
			else {
				none(value)
			}
		}
		else {
			none(value)
		}
	}

	value := 2
	foo := &value
	foo = null
	address := Ref(foo)
	return 0
}
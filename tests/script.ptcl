prototype function printf(content: character*, ...): integer
prototype function printf_number(content: integer): integer
prototype function putchar(content: character): integer

typedata statement(statement_type: integer)

syntax list([name: static type]) end {
    {
        type_name: static word = !<list_ word(name)>
        typedata !type_name()
        syntax list([!name])() end {
            !type_name()
        }
    }

    !type_name
}

function main(): integer {
    other_name: list(character*) = list(character*)()
    return 0
}